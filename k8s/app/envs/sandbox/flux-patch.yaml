---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: voice
  namespace: voice-sandbox
spec:
  template:
    spec:
      $setElementOrder/containers:
      - name: web
      containers:
      - image: itsre/voice-web:sandbox-v0.0.5
        name: web
