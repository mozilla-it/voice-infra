---
apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: voice
  namespace: voice-dev
  annotations:
    fluxcd.io/automated: "true"
    filter.fluxcd.io/chart-image: glob:master-*
spec:
  releaseName: voice-dev

  chart:
    repository: https://mozilla-it.github.io/helm-charts/
    name: mozilla-common-voice
    version: 0.1.2
  values:
    namespace: voice-dev
    voice_web:
      use_secrets_file: true
      use_default_secrets_file: false
      image: itsre/voice-web:master-3b3f41a6268dc73a1347842575cb38e2c9fd4ad5

      ingress:
        enabled: true
        class: voice-dev
        host: dev.voice.mozit.cloud
        additional_host: voice-dev.allizom.org

      service_account:
        annotations:
          eks.amazonaws.com/role-arn: "arn:aws:iam::058419420086:role/voice-web-dev"

      config:
        redis_url: "redis://dev-voice-eks-001.dev-voice-eks.cljunw.usw2.cache.amazonaws.com"
        mysql:
          host: "voice-eks-dev.cd5od9qtbvp0.us-west-2.rds.amazonaws.com"
          root_user: "voice"

    new_relic:
      enabled: true
      app_name: voice-dev

    redis:
      enabled: false
    elasticsearch:
      enabled: false
    kibana:
      enabled: false
    mysql:
      enabled: false
